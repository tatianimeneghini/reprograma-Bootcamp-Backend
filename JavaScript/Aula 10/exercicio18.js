while (arrayAlgoritmo.lenght > 1){
    algoritmo = arrayAlgoritmo.substr(0, (arrayAlgoritmo.length + 1));
    console.log(algoritmo);
}

/*Fazer com for, dentro do outro
Um for define a quantidade de caracteres 
O outro vai construir a linha 