//Calcular desconto
function calcularDesconto(valor){;
  let desconto = valor * 0.05;
  console.log(calcularDesconto(20));
  return desconto;
}

function soma(numero1, numero2){
  console.log(soma(3, 2));
  return soma = numero1 + numero2;
}